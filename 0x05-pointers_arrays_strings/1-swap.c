#include "main.h"
/**
*  reset_to_98 - takes the pointer and updates the value
*  @n: int to check
*  Owned By Bwave/Bright
*  Return: 0 is success
*/
void swap_int(int *a, int *b)
{
	int = c;

	c = *a;
	*a = *b;
	*b = c;
}
